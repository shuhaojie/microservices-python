apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: mp3
spec:
  type: ClusterIP
  selector:
    app: mysql
  ports:
    - name: http
      protocol: TCP
      port: 3306
      targetPort: 3306
